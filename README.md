I think it would be pointless to write a readme content for a project that is not exactly complted the way it was supposed to(i will write readme content to this project later when i will complete this project in true sense).

I have failed as a student to complete this project properly due to having very little experience in building projects. Specifically, this project demands a different approach that I have never used before, involving separation of concerns through different modules, as well as the Single Responsibility Principle (SRP).

So, what I have made is not exactly (or perfectly) how it should have been made, although it is still functioning (but that is not the point). The project I built does not use classes because I am not used to connecting all the dots together at once. Therefore, I prefer going step by step. I first focused on making the project as simple as possible and then planned to structure it into classes later.

However, after completing the project—which took roughly 20 days (I was tired) and ended up being around 400–500 lines of code—I could not categorize everything into different modules due to tiredness and brain rot. I also could not properly apply SRP, and I may have messed up the DRY principle as well.

That said, someday I might come back to this project and refine it to perfection by implementing classes, SRP, and the DRY principle, while also organizing everything into separate modules (the way Webpack projects are typically structured) rather than keeping everything inside a single JavaScript file, which is the current state of the project.

Also note, that this project was never really meant to use composition (in case if you prefer to use for this project), which is generally preferred over inheritance, because the design itself is not intended to be built around inheritance (from what I have figured out).

I also wasted the CSS and style loaders that I installed and configured in package.json and the Webpack configuration files because I ended up placing all of my CSS inside the template.html file within a style tag (again, due to tiredness and peak brain rot). In the future, I will separate the CSS into different files for different modules as well.

I failed because The Odin Project is not primarily about frequent project practice. It focuses more on teaching concepts and exposing learners to many different things. However, when it assigns projects, those projects are often extremely difficult. The reality is that the course is not centered around project practice alone, and because of that, nearly every beginner struggles when it comes to actually building projects in The Odin Project. So it is not just my problem.

With all that said, I would like to go now.